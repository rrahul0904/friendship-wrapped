import { AuthExperience } from "@/components/AuthExperience";
export const metadata = { title: "Sign in — ThreadTales" };
export default function LoginPage() { return <AuthExperience mode="login" />; }
